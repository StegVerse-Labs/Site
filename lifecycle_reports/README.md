# lifecycle_reports

Bundle lifecycle reconciliation reports are written here.

The reconciler classifies whether a bundle or milestone is:

```text
installed_verified
files_present_but_archive_missing
failed_only
partially_installed
missing
conflicted
```

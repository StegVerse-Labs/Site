# Economic ecosystem public roadmap — publication and evidence contract (draft)
Updated: 2026-09-22
Publisher paper candidate: GCAT-BCAT-Engine/Publisher/papers/StegVerse_Private_State_Economy_White_Paper_v0.1.md
Site preview: economic-roadmap.html
Site proposed status projection: data/economy/public-release-benchmarks.v1.json

## Boundaries
- Publisher owns the scholarly white paper and public release posture; Site must not publish the draft as a released paper until canonical governed publication closure is authenticated.
- This Site PR stages presentation and the fail-closed initial all-pending projection. A PR or merge is not a production release, and a static status JSON is not authentic runtime proof.
- Existing canonical Task Registry/COSV, WorkerCoordinator, canonical StegOS runtime lease, Interlock/InTr and Master Records own their existing respective authorities. Do not mint a roadmap-specific authority plane, runtime or scheduler.
- All private MyKV content remains outside the public evidence projection. SKAP credentials, detailed individual task contents, API account identities and personally identifying financial records must never enter the public JSON.

## Benchmark proof contract
An existing machine-owned Publisher projection should update the public benchmark file only after querying the exact canonical benchmark Task ID and corresponding exact state-transition evidence through the existing authorized Master Records binding. Every VERIFIED_COMPLETE benchmark requires:
1. An exact registered benchmark ID mapped to existing canonical task and COSV ownership (new task bindings must enter through canonical Task Registry, not invented locally by Site).
2. Authentic retained result evidence for the specific operational predicate. A source implementation, hosted CI, browser IndexedDB, proposed design, or synthetic fixture is not runtime success.
3. Canonical Master Records state=RECORDED, reconstruction_status=PASS and required_evidence_validation_status=PASS with exact receipt_sha256==reconstructed_receipt_sha256, plus any benchmark-specific constraints (for example genuine native installation).
4. If crossing organizational boundaries, the required authenticated external Interlock/InTr decision and exact scoped organization receipts.
5. Governed Publisher closure binding the release projection request to the verified source, exactly matching output content and intended public target. No direct submitter can mutate the public record under publication authority.
6. A retained authenticated publication result and public readback confirming that the Site received this version.

The public projection records only public-safe benchmark ID, status, verified_at, canonical public evidence reference and status snapshot timestamp. Unavailable or invalid proof is NOT_VERIFIED or UNKNOWN_NOT_AUTHENTICALLY_OBSERVED; never silently promote on CI success. When completed proof is later revoked, superseded, stale or invalidated, refresh the projection to a non-complete status. The public reader never computes completion from implementation percentage.

## Current implementation vs next required work
This draft implements a read-only public webpage and an all-pending status contract with a source checker; it does NOT install a new background polling process, Master Records runtime client, resident scheduler or Publisher mutation capability. The existing Publisher governed publication lane remains the owner of actual promotion. A separate canonical Task Registry work packet must bind these benchmark IDs to established tasks and create the authorized projection handoff. The Publisher must produce an authenticated public-safe evidence export; then its existing governed publication mutation path can update this JSON. Until that authentic integration succeeds, check marks stay empty.

## Editorial process
1. Review this draft white paper against the existing Entity Economy volumes, the FinCo Private-State Value thesis and StegToken/StegCoin reconciliation.
2. Validate legal/investment language separately from product claims. Do not publish token offers or return predictions.
3. Complete exact external InTr ALLOW and canonical Master Records publisher closure, using the existing GOVERNED-WIKI-PUBLICATION-TRANSITION-001 pattern where applicable, without equating a reviewed candidate to publication.
4. Publish the white paper from canonical Publisher and mirror through the existing Site paper-mirror flow; only then enable its public reading link and navigation.
5. Enable the roadmap's automatic check-off when the canonical public-safe proof projection is authentic. Observe actual deployed page propagation and fail visibly on inaccessible status.

## Product stage policy
Stage 1 is MyKV WorkSpace with private AI Assistant plus Ecosystem Chat participants ChatGPT (first), Claude Code (second), followed by honest free-tier cost benchmarking. Stages 2–6 are purpose-limited private-state commerce, human–AI marketplace, lawful settlement/rewards, investment/liquidity research and globally federated economic participation. Financial stages cannot be promoted on hypothetical simulations alone.

## Stage 1 correction — ephemeral external AI runs independently of native MyKV (2026-09-23)

The approved Stage 1 execution plan has TWO PARALLEL LANES:

- **External AI / ephemeral StegBrowser:** an existing retained StegBrowser node admits a bounded ephemeral lease, obtains a fresh authorized task and exact InTr ingress decision, and invokes an approved OpenAI or Anthropic provider path through the existing LLM Adapter with TV/TVC/SKAP execution-scoped credentials. A genuine model response must be returned as usable output to the requesting StegVerse task, with independent per-provider attribution, usage accounting, exact governed egress and durable organization/Master Records evidence. The disposable browser lease then terminates and proves destruction without deleting retained node or task continuity. Native MyKV installation is **not** an admission prerequisite for this lane; authorization can instead scope a task with non-private or otherwise approved inputs. An ephemeral session is not a locally installed copy of OpenAI or Anthropic's model.
- **Private MyKV WorkSpace / AI Assistant:** separately establish genuine installed iPhone/device-resident KV, user-controlled personal context and the native private assistant. When available, explicit scoped authorization can send bounded context to an external AI lane and return an approved result, without making the personal KV a public provider input or requiring a second user-operated device.

Preserve separate benchmarks **S1_NATIVE_KV**, **S1_MYKV_AI**, **S1_CHATGPT** and **S1_CLAUDE** with zero verified statuses until each own authentic runtime predicate is proven. The public projection's `stage1_execution_lanes` and external gate `required_proof` are descriptive source contracts, not a new runtime or execution authorization. The direct Anthropic Messages API supports Claude responses; use of Claude Code as an agent/tool additionally requires its own approved interface and cannot be claimed from a Messages API receipt alone. A StegBrowser web-site login automation route must not be silently substituted for governed provider API ingress.

Reuse canonical existing owners: StegBrowser's bounded leases, retained node and terminal destruction; existing SDK manifestation and WorkerCoordinator claim/fence; TV/TVC/SKAP credential control; the OpenAI/Anthropic provider adapters; Interlock/InTr ingress/egress; organizational receipts and canonical Master Records custody. The first complete real external-AI task can be achieved before native MyKV packaging, although completing ALL Stage-1 benchmarks ultimately requires both foundations.

Current first external-AI runtime predicate remains **UNKNOWN_NOT_AUTHENTICALLY_OBSERVED**. Existing StegBrowser navigation/source tests and LLM Adapter fixture tests do not prove an end-to-end admitted provider call or a reconstructable result. Do not fabricate proof or automatically check the dashboard before it exists.

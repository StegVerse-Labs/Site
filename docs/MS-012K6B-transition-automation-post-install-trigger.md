# MS-012K.6B Transition Automation Post-Install Trigger

This bundle triggers the installed transition automation controller through `bundle-manifest.json`.

```text
post_install_tasks:
  - data/headless-tasks/transition-automation-controller-v1.json
```

Use only after MS-012K.6 is active.

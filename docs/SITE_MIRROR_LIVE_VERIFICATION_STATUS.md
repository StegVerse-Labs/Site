# Site Mirror Live Verification Status

## Source of Truth

`docs/SITE_MIRROR_HANDOFF.md` is the current handoff and task source of truth.

## Current Activation State

```text
ready_for_live_mirror_verification
```

## Current Finding

The Site mirror hardening files exist, and the mirror generator/checker code is present. The current checked-in `papers/papers_manifest.json` still appears to be an older generated manifest because it contains `source` and `target` fields but not the complete required source metadata field set from the handoff contract.

## Required Before Activation

```text
Publisher dry-run workflow URL
Publisher live dispatch workflow URL
Site mirror workflow URL
Site commit SHA generated by mirror workflow
papers/papers_manifest.json source_repository
papers/papers_manifest.json source_ref
papers/papers_manifest.json source_of_truth
public alias verification results
Publisher receipt update commit
Publisher verification tracker activation commit
```

## Current Blocker

```text
activation_blocked: true
reason: live mirror verification evidence and regenerated source-metadata manifest are not fully captured yet
```

## Next Build Step

Run or trigger the Publisher-to-Site mirror path in the required order, then use:

```text
python scripts/check_site_mirror_activation_evidence.py
python scripts/check_papers_manifest_metadata.py
python scripts/check_paper_display_policy.py
```

Only after those pass should Publisher update its receipt and verification tracker to activated.

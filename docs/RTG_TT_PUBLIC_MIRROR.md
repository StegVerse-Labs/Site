# RTG–TT Public Mirror

## Status

`CANDIDATE_PARALLEL_SAFE`

This Site package begins the public projection of the canonically merged `rtg-tt-v0.1` integration boundary.

It is not activated merely because these files exist. Activation requires Site orchestration admission, validator success, merge verification, and exact downstream ingestion.

## Coordination Authority

Tracking issue: `StegVerse-Labs/Site#122`

The active Site sequence remains owned by the HIL upload task on its declared paths. This mirror claims only:

```text
data/rtg-tt/
docs/RTG_TT_PUBLIC_MIRROR.md
scripts/check_rtg_tt_public_mirror.py
.github/workflows/rtg-tt-public-mirror.yml
```

It does not claim:

```text
humans-as-interoperability-layer.html
assets/hil-*
scripts/check_hil_*upload*
```

## Canonical Sources

```text
Data-Continuation/RTG-Tests
  2fd7099bb3b6d2d7aa8ce7d72f7f71f6dfd4566a

Admissible-Existence/TT
  19720bae935ef9881ce5b239e0b66423fcd8bfab

Admissible-Existence/ae-validation-factory
  5ca6ac0442809c5902f9765d2f865e5fa03d9c60

Admissible-Existence/AE public index
  PR #5
  head e0fe6e8d878ea4030ae8c41fcbfaee7b9b72c58a
```

Factory report:

```text
sha256:a11eea46242f3df6faa64c2c4174779abea08be9ca7ea93342317cdbcdf7aa56
```

## Ownership Boundary

RTG/AE owns relational geometry, candidate significance, transition blocks, constellations, and the geometric minimum-node declaration.

TT owns bounded request consumption, local allocation, the local `ALLOW` / `DENY` / `DEFER` result, and deterministic TT receipts.

The AE Validation Factory owns procedural correlation and consistency checks. It creates no authority.

Site owns only the validated public projection.

## Mapping

```text
RESOLUTION_SATISFIED -> ALLOW
FAIL_CLOSED          -> DENY
QUARANTINE           -> DEFER
```

Execution authorization remains false at this boundary.

## Activation Gates

The layer becomes active only when all of the following are true:

1. Site orchestration admits the package as non-conflicting work.
2. `scripts/check_rtg_tt_public_mirror.py` passes.
3. The Site package merges and is verified on `main`.
4. The admissibility wiki consumes the formal ownership and non-claim boundary.
5. The guardian wiki consumes the authority/refusal boundary.
6. Publisher consumes the Site-validated projection with exact canonical commit bindings.
7. No downstream consumer silently rewrites canonical semantics.

## Non-Claims

This mirror does not claim mathematical completeness, universal physical validity, execution authority, commitment authority, custody, publication authority, or release authority.

An `ALLOW` result is a bounded TT result. It is not independent authorization to execute.

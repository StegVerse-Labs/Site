# Site Mirror Closure Next Build

## Purpose

This document defines the next build surface for Site mirror activation closure without overclaiming activation.

The Site repository can prepare, validate, write, and upload Site-side evidence. Final mirror activation remains blocked until Publisher-side closure consumes both Publisher and Site evidence and updates the Publisher verification tracker and activation status.

## Source Of Truth

```text
Primary handoff: docs/SITE_MIRROR_HANDOFF.md
Repository: StegVerse-Labs/Site
Source repository: GCAT-BCAT-Engine/Publisher
Source path: papers
Target path: papers
Current boundary: closure evidence pending
```

## Current Pending Evidence

```text
Publisher workflow run URL
Publisher verification receipt artifact
Publisher live dispatch workflow URL
Site mirror workflow URL
Site mirror commit SHA generated by workflow
Site evidence artifact
Publisher closure nudge result
Publisher closure receipt
Publisher verification tracker activation commit
Publisher activation-status update commit
```

## Site-Side Closure Preparation

The Site repository may prepare the following without claiming activation:

```text
1. Preserve Publisher as source of truth.
2. Preserve source metadata in papers/papers_manifest.json.
3. Generate Site mirror evidence packet.
4. Generate Site live evidence state JSON.
5. Upload the Site evidence artifact.
6. Nudge Publisher closure workflow when cross-repo credentials are available.
7. Keep scheduled Publisher closure as fallback when credentials are unavailable.
8. Record the exact remaining Publisher-side closure requirements.
```

## Non-Activation Rule

The Site repository must not mark the mirror activated solely because Site evidence exists.

Activation requires Publisher closure evidence:

```text
1. Publisher automated dispatch workflow produces a verification receipt artifact.
2. Publisher workflow dispatches Site mirror workflow successfully.
3. Site mirror workflow completes.
4. Site mirror workflow writes and uploads Site evidence artifact.
5. Publisher closure workflow consumes Publisher and Site evidence artifacts.
6. Publisher closure workflow writes docs/mirror-activation-closures/<closure>.json.
7. Publisher verification tracker is updated to activated.
8. Publisher activation status is updated to activated.
```

## Next Build Candidate

The next build candidate is not a new paper-display feature. It is a closure-readiness guard that keeps Site evidence, handoff status, live evidence state, and Publisher closure prerequisites aligned.

## Completion Condition

This packet is complete when:

```text
python scripts/check_site_mirror_closure_next_build.py
```

passes and the handoff continues to identify the same pending closure boundary.

## Archive Readiness

This file is sufficient for a future session to continue Site closure-readiness work without requiring the prior chat thread.

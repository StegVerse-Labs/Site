# Site bounded claim records

Per-task claim files supplement, but do not replace, the canonical `data/session-work-claims.json` registry. Canonical collision admission remains governed by that registry and the Site pre-work gate.

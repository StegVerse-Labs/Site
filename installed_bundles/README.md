# installed_bundles

Successfully ingested bundles are archived here.

Each installed bundle should have:

```text
<bundle-name>.zip
<bundle-name>.installed.json
<bundle-name>.installed.md
```

This directory distinguishes accepted/applied transition objects from `incoming/`, `failed_bundles/`, `sandbox_queue/`, and `privileged_queue/`.

(() => {
  'use strict';
  const STORAGE_KEY='stegverse.iphone-heartbeat-transition-receipt.v1';
  const EXPECTED_ORIGIN='https://stegverse.org';
  const CONTRACT_ID='SHWP-IPHONE-HB30-TRANSITION-CAPSULE-001';
  const LEGACY_BLOB='d18d57d83cf19b7799cde1a1b4487e496eca7f76';
  const environmentStatus=document.getElementById('environment-status');
  const generateButton=document.getElementById('generate-receipt');
  const copyButton=document.getElementById('copy-receipt');
  const shareButton=document.getElementById('share-receipt');
  const downloadButton=document.getElementById('download-receipt');
  const resultStatus=document.getElementById('result-status');
  const output=document.getElementById('receipt-output');
  function canonicalize(value){if(Array.isArray(value))return`[${value.map(canonicalize).join(',')}]`;if(value&&typeof value==='object')return`{${Object.keys(value).sort().map(k=>`${JSON.stringify(k)}:${canonicalize(value[k])}`).join(',')}}`;return JSON.stringify(value)}
  function toHex(bytes){return Array.from(new Uint8Array(bytes),b=>b.toString(16).padStart(2,'0')).join('')}
  async function sha256Hex(text){return toHex(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text)))}
  function browserEvidence(){const width=Number(screen&&screen.width)||Number(innerWidth)||0;const height=Number(screen&&screen.height)||Number(innerHeight)||0;const touch=Number(navigator.maxTouchPoints||0);const ua=String(navigator.userAgent||'');const sides=[width,height].sort((a,b)=>a-b);const iphoneClassEvidence=ua.includes('iPhone')||(touch>=2&&sides[0]>0&&sides[0]<=500&&sides[1]<=1000);return{origin:location.origin,user_agent:ua,platform:String(navigator.platform||''),max_touch_points:touch,screen_width_css:width,screen_height_css:height,iphone_class_evidence:iphoneClassEvidence,secure_context:window.isSecureContext===true,webcrypto:Boolean(globalThis.crypto&&crypto.subtle&&typeof crypto.subtle.digest==='function')}}
  function environmentCheck(){const failures=[];const evidence=browserEvidence();if(location.origin!==EXPECTED_ORIGIN)failures.push(`origin must be ${EXPECTED_ORIGIN}`);if(!evidence.iphone_class_evidence)failures.push('current browser must provide iPhone-class touch/screen evidence');if(window.isSecureContext!==true)failures.push('secure browser context required');if(!globalThis.crypto||!crypto.subtle||typeof crypto.subtle.digest!=='function')failures.push('WebCrypto SHA-256 required');return failures}
  function unsignedReceipt(){return{schema:'stegverse.iphone-heartbeat-transition-receipt/v1',contract_id:CONTRACT_ID,physical_execution_surface:'CURRENT_USER_IPHONE',executed_at:new Date().toISOString(),seed:{repository:'StegVerse-Labs/.github',legacy_state_ref:'control/heartbeat-state.json',legacy_state_git_blob_sha:LEGACY_BLOB,epoch:29,generation:29},successor:{schema:'stegverse.heartbeat-carrier-runtime-state/v1',epoch:30,generation:30,reference_frame:'heartbeat_epoch:30',activation_state:'ACTIVE',authority_effect:'NONE',legacy_hb29_immutable:true},authority:{credential_authority:'TV/TVC',credential_requirement:'NONE',github_token_runtime_authority:'NONE',non_tv_tvc_secret_or_token_used:false,worker_authority:false,claim_or_fence_mutation:false,route_authority:false,wallet_authority:false,model_output_authority:'NONE',hosted_runtime_production_authority:'NONE',another_physical_machine_required:false},browser:browserEvidence()}}
  async function generateReceipt(){const failures=environmentCheck();if(failures.length){resultStatus.textContent=`FAIL_CLOSED: ${failures.join('; ')}`;return}generateButton.disabled=true;try{const receipt=unsignedReceipt();receipt.receipt_sha256=await sha256Hex(canonicalize(receipt));const formatted=JSON.stringify(receipt,null,2);localStorage.setItem(STORAGE_KEY,formatted);output.value=formatted;resultStatus.textContent=`PORTABLE_RECEIPT_READY — sha256:${receipt.receipt_sha256}`;copyButton.disabled=false;shareButton.disabled=false;downloadButton.disabled=false}catch(error){resultStatus.textContent=`FAIL_CLOSED: ${error instanceof Error?error.message:String(error)}`;output.value=''}finally{generateButton.disabled=false}}
  async function copyReceipt(){if(!output.value)return;await navigator.clipboard.writeText(output.value);resultStatus.textContent='Receipt copied locally. Independent canonical verification is still required.'}
  async function shareReceipt(){if(!output.value)return;if(typeof navigator.share!=='function'){resultStatus.textContent='Web Share is unavailable. Use Copy receipt or Save receipt file.';return}const blob=new Blob([output.value],{type:'application/json'});const file=new File([blob],'stegverse-hb29-hb30-iphone-receipt.json',{type:'application/json'});const payload={title:'StegVerse HB29→HB30 portable receipt',text:'Non-authorizing physical transition evidence.',files:[file]};if(navigator.canShare&&!navigator.canShare({files:[file]}))delete payload.files;await navigator.share(payload)}
  function downloadReceipt(){if(!output.value)return;const blob=new Blob([output.value],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='stegverse-hb29-hb30-iphone-receipt.json';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
  const failures=environmentCheck();if(failures.length){environmentStatus.textContent=`FAIL_CLOSED — ${failures.join('; ')}`}else{environmentStatus.textContent='READY — iPhone-class physical evidence, stegverse.org HTTPS, secure context, and WebCrypto verified.';generateButton.disabled=false}
  const retained=localStorage.getItem(STORAGE_KEY);if(retained){output.value=retained;resultStatus.textContent='A prior local portable receipt is retained on this browser. Generate a new receipt only if canonical HB29 remains current.';copyButton.disabled=false;shareButton.disabled=false;downloadButton.disabled=false}
  generateButton.addEventListener('click',generateReceipt);copyButton.addEventListener('click',()=>copyReceipt().catch(e=>{resultStatus.textContent=`FAIL_CLOSED: ${e}`}));shareButton.addEventListener('click',()=>shareReceipt().catch(e=>{resultStatus.textContent=`FAIL_CLOSED: ${e}`}));downloadButton.addEventListener('click',downloadReceipt);
})();

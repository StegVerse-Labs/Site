# headless_cmd_reports

Headless command receipts and Markdown reports are written here.

The declared-task pattern keeps workflow YAML stable. Workflows should dispatch a task runner, while task definitions under `data/headless-tasks/` define the mutable operational logic.

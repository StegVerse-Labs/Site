name: VA PII Detection Evaluation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'data/va-claim-assistant/fixtures/pii-detection-evaluation.json'
      - 'data/va-claim-assistant/pii-detection-evaluation-receipt.json'
      - 'scripts/evaluate_va_pii_detection_fixture.py'
      - '.github/workflows/va-pii-detection-evaluation.yml'

permissions: {}

concurrency:
  group: va-pii-detection-evaluation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Refuse credential-bearing environment
        shell: bash
        run: |
          set -euo pipefail
          for name in GITHUB_TOKEN GH_TOKEN GITHUB_PAT TVC_EPHEMERAL_GITHUB_TOKEN STEGVERSE_PROVIDER_TOKEN STEGVERSE_MASTER_RECORDS_TOKEN ACTIONS_ID_TOKEN_REQUEST_TOKEN; do
            if [ -n "${!name:-}" ]; then
              echo "forbidden credential environment variable present: ${name}" >&2
              exit 1
            fi
          done
          echo 'VA_PII_DETECTION_EVALUATION_CREDENTIAL_AUTHORITY=TV_TVC'
          echo 'VA_PII_DETECTION_EVALUATION_GITHUB_TOKEN_AUTHORITY=NONE'

      - name: Fetch exact Site source anonymously
        shell: bash
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          git -c credential.helper= -c http.extraheader= fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
          test -z "$(git config --local --get-regexp 'extraheader|credential' || true)"

      - name: Recompute and verify deterministic evaluation receipt
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/evaluate_va_pii_detection_fixture.py
          python3 - <<'PY'
          import json
          from pathlib import Path
          receipt = json.loads(Path('data/va-claim-assistant/pii-detection-evaluation-receipt.json').read_text(encoding='utf-8'))
          assert receipt['state'] == 'PASS'
          assert receipt['reference_evaluator_only'] is True
          assert receipt['production_detector_ready'] is False
          assert receipt['private_document_upload_enabled'] is False
          assert receipt['authority_effect'] is False
          assert receipt['activation_effect'] is False
          print('VA_PII_DETECTION_EVALUATION=PASS')
          print('VA_PII_DETECTION_PRODUCTION_READINESS=false')
          print('VA_PII_DETECTION_AUTHORITY_EFFECT=NONE')
          print('VA_PII_DETECTION_ACTIVATION_EFFECT=NONE')
          PY
          git diff --exit-code -- data/va-claim-assistant/pii-detection-evaluation-receipt.json

      - name: Confirm validation-only carrier
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          workflow = Path('.github/workflows/va-pii-detection-evaluation.yml').read_text(encoding='utf-8')
          forbidden = [
              'schedule' + ':',
              'actions/' + 'checkout@',
              'actions/' + 'setup-python@',
              'actions/' + 'upload-artifact@',
              'git ' + 'push',
          ]
          assert 'permissions: {}' in workflow
          assert all(marker not in workflow for marker in forbidden), forbidden
          print('VA_PII_DETECTION_HOSTED_SCHEDULE=RETIRED')
          print('VA_PII_DETECTION_REPOSITORY_WRITEBACK=NONE')
          print('VA_PII_DETECTION_ARTIFACT_CUSTODY=NONE')
          print('VA_PII_DETECTION_RUNTIME_AUTHORITY=NONE')
          PY
